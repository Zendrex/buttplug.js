---
title: Presets
description: Built-in pattern presets and usage
---

7 built-in presets. The `type` column shows the default loop behavior.

<TypeTable type={{
  pulse: { type: 'true', description: 'Square wave on/off' },
  wave: { type: 'true', description: 'Smooth sine wave oscillation' },
  ramp_up: { type: 'false', description: 'Gradual increase to maximum' },
  ramp_down: { type: 'false', description: 'Gradual decrease to zero' },
  heartbeat: { type: 'true', description: 'Ba-bump heartbeat rhythm' },
  surge: { type: 'false', description: 'Build to peak then release' },
  stroke: { type: 'true', description: 'Full-range position strokes' },
}} />

## Preset Pattern Descriptor

<auto-type-table path="src/patterns/types.ts" name="PresetPattern" />

## Usage

```ts
// Shorthand (recommended)
await engine.play(device, "heartbeat");

// With options
await engine.play(device, "heartbeat", {
  intensity: 0.8,  // 0-1 amplitude scale
  speed: 1.5,      // 0.25-4x speed multiplier
  loop: true,
});
```

<Accordions>
<Accordion title="Full Descriptor Form">
```ts
await engine.play(device, {
  type: "preset",
  preset: "heartbeat",
  intensity: 0.8,
  speed: 1.5,
  loop: true,
});
```
</Accordion>
</Accordions>

Intensity scales all keyframe values by the given multiplier (e.g. `0.5` = half intensity, `1.0` = full). Applies to both preset and custom patterns.
